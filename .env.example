DATABASE_URL="postgresql://memo:YOUR_PASSWORD@localhost:5433/memo?schema=public"
JWT_SECRET="your-jwt-secret-here"
JWT_REFRESH_SECRET="your-refresh-secret-here"
POSTGRES_PASSWORD="changeme"

# Web Push (generate with: npx web-push generate-vapid-keys --json)
VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
VAPID_EMAIL="mailto:your-email@example.com"

# Frontend (must match VAPID_PUBLIC_KEY)
VITE_VAPID_PUBLIC_KEY="your-vapid-public-key"

# OpenAI (for AI health analysis)
OPENAI_API_KEY="sk-your-openai-api-key"
